# Node modules
node_modules

# Adonis directory for storing tmp files
tmp

# Environment variables, never commit this file


# The development sqlite file
database/*.sqlite

#Docker on deleveloper mode, not up still

Dockerfile
docker-compose.yml
.dockerignore
